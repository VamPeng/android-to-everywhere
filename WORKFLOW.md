---
type: workflow
updated: 2026-08-23
---

# 执行规则

## 1. 唯一信息源

- `main` 分支中的 Markdown 是规划与复盘的唯一权威来源。
- Obsidian 只是直接编辑和展示本地 Git 仓库，不复制第二份数据。
- 实际代码保存在各技术项目仓库，本仓库只保存链接、结论和证据索引。

## 2. 四级计划

```text
长期路线 ROADMAP
→ 季度成果 quarters/
→ 每周交付 weeks/
→ 代码、案例、测试与数据
```

年度方向可以粗，季度目标必须可验收，每周任务必须能完成并留下证据。

## 3. 每周任务上限

默认只保留三类交付物：

1. 一个 Android 深度任务。
2. 一个 Flutter 或 RN 主攻任务。
3. 一个维护/扩展任务，可来自另一跨端框架、iOS、HarmonyOS、Vue、Java 或工程质量。

任务不能写成“学习 Flutter”“学习 JVM”。应写成：

> 使用 Flutter 完成支持校验、加载、错误和重试状态的登录流程，并留下测试与代码说明。

## 4. Flutter / RN 轮换

- 两者战略优先级一致。
- 每个 Sprint 只选一个作为主交付，另一个只做保持任务。
- 下个 Sprint 交换主次。
- 两边使用相同业务模型和验收标准，方便比较与迁移。

## 5. 实战闭环

```text
目标与场景
→ 自己先写方案
→ 实现或故意制造问题
→ 获取日志/堆栈/Trace/测试证据
→ 定位根因
→ 修复或交付
→ 数据验证
→ 记录可迁移结论
```

没有证据的“感觉更快”“应该没问题”不能作为结论。

## 6. AI 使用协议

使用 AI 前：

- 先写清数据流、模块边界和验收条件。
- 能说明准备让 AI 完成哪一部分，以及自己负责验证什么。

AI 生成后：

- 逐段解释关键调用链和状态变化。
- 至少手动修改一次需求或异常分支。
- 主动制造一个错误并完成排查。
- 为关键逻辑补测试或可重复验证步骤。
- 记录不能解释的部分，不能直接标记为掌握。

## 7. 完成定义

每项交付至少包含：

- 可运行代码或可访问结果；
- 一段关键实现说明；
- 一个错误/边界场景；
- 测试、Trace、日志、截图或发布记录中的至少一种证据；
- 对应 Commit、PR 或仓库链接。

## 8. 周节奏

```text
周初：从季度目标中选 3 个交付物
周中：只更新证据和阻塞，不扩张范围
周末：完成复盘，提交 Markdown，调整下一周
季度末：重新评估技能矩阵和优先级
```
