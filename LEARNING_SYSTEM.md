---
type: learning-system
updated: 2026-08-23
---

# 实战学习系统

## 为什么需要这一层

职业路线中的任务不能默认“已经会使用工具，只差做一次案例”。当某项能力的当前基线是不会独立分析时，必须先提供教学材料、受控实验和判读方法，再要求独立完成案例。

因此，后续所有新能力使用四阶段推进：

```text
L0 学习包：概念地图、工具职责、官方资料、判读清单
→ L1 引导实验：已知故障 + 明确操作步骤 + 观察目标
→ L2 半开放案例：只给现象，不直接给根因
→ L3 独立案例：真实问题或未知故障，完整定位、修复和复盘
```

## 四个阶段的完成标准

### L0 学习包

必须回答：

- 这个问题为什么会发生；
- 应该先用哪个工具，为什么；
- 工具中需要看哪些轨道、指标或调用链；
- 常见错误结论是什么；
- 本次实验最终需要回答哪些问题。

只阅读资料不算完成，必须能用自己的语言复述分析路径。

### L1 引导实验

目的不是考察是否能猜出根因，而是学习工具与证据之间的对应关系。

必须包含：

- 可稳定复现的已知故障；
- 自定义 Trace 标记或其他可识别证据；
- 明确的采集步骤；
- 一份待填写的观察表；
- 修复前后对比。

### L2 半开放案例

只提供症状、复现路径和验收条件，不直接告诉根因。允许提供分层提示：

```text
提示 1：应该先看哪类工具
提示 2：重点检查哪条线程或轨道
提示 3：可能涉及的代码区域
```

完成后必须解释排除过哪些错误假设。

### L3 独立案例

以真实项目、公开实验项目或隐藏故障分支为载体，独立完成：

```text
现象 → 复现 → 采证 → 假设 → 验证 → 根因 → 修复 → 回归
```

只有达到 L2/L3，才能把能力更新为“可独立排查”。

## AI 在学习过程中的责任

每次进入新主题时，AI 需要：

1. 优先搜集官方文档、源码与一手资料；
2. 输出面向当前能力基线的中文学习文档；
3. 提供可运行的受控实验，而不是只布置结果任务；
4. 告诉用户工具中具体看什么、如何判断；
5. 根据用户提交的截图、Trace、日志或结论进行逐项纠正；
6. 在用户真正解释清楚之前，不把任务标记为掌握；
7. 将最终结论、证据和下一步同步回仓库。

## 用户在学习过程中的责任

用户不需要在开始前就知道答案，但需要：

- 实际运行实验并采集证据；
- 先按观察表给出自己的判断；
- 标记无法理解的轨道、字段或调用链；
- 根据反馈重新检查证据；
- 最终能够脱离答案复述分析过程。

## 文档位置

```text
learning/
├── README.md
└── android-performance/
    ├── README.md
    └── 01-ui-jank-guided-lab.md
```

后续每个方向都采用同样结构，例如：

```text
learning/flutter/
learning/react-native/
learning/ios/
learning/harmonyos/
learning/vue/
learning/java-backend/
```

学习文档负责“怎么学”；`weeks/` 负责“这周交付什么”；`cases/` 负责“最终证明了什么”。